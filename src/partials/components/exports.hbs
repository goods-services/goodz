{{#eq styleType 'Styled Components'}}
export { {{pascalCase name}} as {{pascalCase name}}Vanlla };
{{/eq}}
{{#eq styleType 'Material Design'}}
export { {{pascalCase name}} as {{pascalCase name}}Vanlla };
{{/eq}}
{{#if isConnected}}
export default connect(
{{#ifIn 'mapStateToProps' connectedFunctions}}
mapStateToProps,
{{else}}
null,
{{/ifIn}}
{{#ifIn 'mapDispatchToProps' connectedFunctions}}
mapDispatchToProps,
{{else}}
null,
{{/ifIn}}
)({{pascalCase name}}Component)
{{else}}
export default {{pascalCase name}}Component;
{{/if}}
