import React from 'react';
import Layout from '@organisms/Layout';

/**
 * Blog Page
 * @param {object} props Incoming Props
 * @returns {XML} A Blog Page
 */
const BlogPage = () => {
  return (
    <Layout >
      <h1>This is a blog template!</h1>
    </Layout>
  );
};

export default BlogPage;
